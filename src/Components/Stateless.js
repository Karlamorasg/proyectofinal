import React from 'react';

const Stateless = () => { // componente que solo sirve para mostrar, no tiene estado ni ciclo de vida
    return (
        <div>
            
        </div>
    )
};

export default Stateless;
